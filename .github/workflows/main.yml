name: Verify Human Identity
on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'Nama Agent'
        required: true
        default: 'ayye'
      agent_description:
        description: 'Deskripsi Agent'
        required: true
        default: 'ayye AI Agent'

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Step 1 - Install Skill
        run: npx clawhub@latest install verified-agent-identity

      - name: Install Dependencies
        run: |
          cd scripts
          npm install

      - name: Step 2 - Setup Identity
        # Kita pakai --key agar tidak error 'exit 1' karena identity kosong
        run: |
          node scripts/createNewEthereumIdentity.js --key ${{ secrets.ETH_PRIVATE_KEY }}

      - name: Step 3 - Generate Link
        run: |
          node scripts/manualLinkHumanToAgent.js --challenge '{"name": "${{ github.event.inputs.agent_name }}", "description": "${{ github.event.inputs.agent_description }}"}'
